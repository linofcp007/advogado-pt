node_modules/
# `dist/index.js` é o bundle self-contained versionado (plugin via marketplace);
# o resto de dist/ (saída do tsc, só para testes) fica ignorado.
dist/*
!dist/index.js
# `content/` é versionado (lido em runtime pelo servidor); regenerado por `npm run build`.
*.tgz
